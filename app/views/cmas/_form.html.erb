<%= form_for(@cma) do |f| %>
  <% if @cma.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cma.errors.count, "error") %> prohibited this cma from being saved:</h2>

      <ul>
      <% @cma.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
